spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:library
  sql:
    init:
      platform: h2
      mode: always
#      dataLocations: classpath:db/schema-h2.sql
server:
  port: 10800
